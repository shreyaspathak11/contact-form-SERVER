{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@now/node-server" }],
  "routes": [
    { "src": "/send-email", "dest": "/server.js" },
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
